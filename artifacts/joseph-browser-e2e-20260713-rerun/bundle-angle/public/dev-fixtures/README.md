Place local Studio-only test videos here.

Recommended filename:

- `public/dev-fixtures/test-video.mp4`

Then open the `MaleHeadVideoLongFormDevFixture` composition in Remotion Studio.

That composition reads the browser-safe public URL:

- `/dev-fixtures/test-video.mp4`
- caption proof fixture: `src/data/dev-fixtures/test-video.words.json`

This is only a local development fixture path. It is not a backend upload flow.
