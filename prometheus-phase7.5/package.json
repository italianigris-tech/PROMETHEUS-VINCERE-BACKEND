{
  "name": "prometheus-phase7.5",
  "version": "7.5.0",
  "description": "Prometheus Phase 7.5 — The Unicorn Learning Layer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
